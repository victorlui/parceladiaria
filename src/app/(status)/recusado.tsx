import Layout from "@/shared/components/Layout";
import React from "react";
import { StyleSheet, Text } from "react-native";

export default function recusado() {
  return (
    <Layout>
      <Text>recusado</Text>
    </Layout>
  );
}

const styles = StyleSheet.create({});
